{
  "name": "habit-athlete-pwa",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Checklist diario pessoal com treino, agua, sono e historico local.",
  "scripts": {
    "generate:assets": "node ./scripts/generate-assets.mjs",
    "build": "node ./scripts/build.mjs",
    "preview": "node ./scripts/preview.mjs dist 4173",
    "dev": "node ./scripts/preview.mjs . 4173",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test:fast": "node --test tests/unit/*.test.js",
    "test:artifact": "node --test tests/artifact/*.test.js",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test": "npm run typecheck && npm run test:fast && npm run test:artifact && npm run test:e2e"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "@playwright/test": "^1.59.1",
    "fake-indexeddb": "^6.2.5",
    "typescript": "^5.9.3"
  }
}
